import styles from '@/styles/projects/moBlog.module.css'

export default function MoBlog({ url, ...props }) {
  return (
    <div className={styles.container} {...props}>
      MoBlog - to be coded
    </div>
  )
}
