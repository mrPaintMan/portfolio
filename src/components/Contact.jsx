import Section from './Section'
import styles from '@/styles/contact.module.css'

export default function Contact(props) {
  return (
    <Section title={'Contact'} CCW {...props}>
      <div className={styles.container}>
        To be coded!
      </div>
    </Section>
  )
}
